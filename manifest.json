{
  "name": "Auth Token",
  "description": "Provides tools for generating/validating temporary authentication tokens in order to grant access to site content rather than using a username and password.",
  "version": "2.0",
  "core": "5.x",
  "dependencies": [
    "crypto"
  ]
}